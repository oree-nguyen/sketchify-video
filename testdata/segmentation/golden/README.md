Golden simple-background outputs are generated by the frozen standard branch
test harness. The object block list (bbox, pixels and unit type) remains the
regression surface; Phase 1 adds a residual coverage layer around it without
changing those object values. Do not hand-edit golden data; update only with
an explicit baseline migration record and a byte/hash comparison.
